<nav class="navbar  navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <div class="col-sm-3">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to image_tag("2.png"),'/', width: 30, class: "navbar-brand"%>
          </div>
        </div>
    <!-- Brand and toggle get grouped for better mobile display -->
        <div class="col-sm-9">
           <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
              <ul class="nav navbar-nav">

                <% unless user_signed_in? %>
                  <li class="<%="active" if current_page?("/users/sign_up")%>"><%= link_to "Sign Up", new_user_registration_path %></li>
                  <li class="<%="active" if current_page?("/users/sign_in")%>"><%= link_to "Sign In", new_user_session_path %></li>
                <% end %>
              </ul>
              
              <ul class="nav navbar-nav navbar-right">
                <li class="<%="active" if current_page?("/events")%>"><%= link_to "Events", events_path %></li>
                <% admins_only do %>
                  <li class="<%="active" if current_page?("/admin")%>"><%= link_to "Dashboard", admin_root_path %></li>
                <% end %>
                
                <% if user_signed_in? %>
                  <li class="<%="active" if current_page?("/users/sign_up")%>">
                    <%= link_to edit_user_registration_path do %>
                      <%= image_tag avatar_url(current_user), width: 25, class: "img-circle"%> <%= current_user.username %>
                    <% end %>
                  </li>
                  <li class="<%="active" if current_page?("/users/#{current_user.username}")%>"><%= link_to "My Events", user_path(current_user)%></li>
                  <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
                  
                <% end %>
              </ul>
            </div><!-- /.navbar-collapse -->
        </div>
   
  </div><!-- /.container-fluid -->
</nav>